Addtable.exe is a command-line utility for appending binary table files to an existing TrueType font.

For example, the utility can be used to add the TrueType Open tables generated by the TTO Assembler.

The syntax for using addtable.exe follows:

ADDTABLE <table name> <table file> <old TTF> <new TTF>